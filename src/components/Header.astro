---
// interface Props {
// 	className: string;
// }

// const { className } = Astro.props;
const mainPagesLinks = [
  {
    href : "/",
    label : "Accueil"
  },
  {
    href : "/projets",
    label : "Projets"
  },
  {
    href : "/a-propos",
    label : "A Propos"
  }
]
---

<header class=" bg-slate-400 bg-opacity-10 w-full h-20 flex flex-row justify-between px-8">

  <h1 class="self-center font-sans font-medium text-xl">Antoine Jézéquel</h1>

  <ul class=" flex flex-row gap-8 h-full align-middle justify-end" >
    {
      mainPagesLinks.map( link => {
        return <li class="flex items-center justify-center" >
          <a href={link.href} class="h-fit font-sans" >{ link.label }</a>
        </li> 
      })
    }
  </ul>
</header>